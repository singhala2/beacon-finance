import { StubPage } from '@/components/dashboard/StubPage';

export default function SettingsPage() {
  return (
    <StubPage
      phase="Phase 5"
      title="Settings"
      description="Profile, integrations, data export, and account deletion. Phase 5."
    />
  );
}
