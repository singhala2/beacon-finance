import { StubPage } from '@/components/dashboard/StubPage';

export default function GoalsPage() {
  return (
    <StubPage
      phase="Phase 5"
      title="Goals"
      description="Goal detail pages with projection charts and contribution suggestions land in Phase 5. The dashboard goals card is built in milestone 2B."
    />
  );
}
