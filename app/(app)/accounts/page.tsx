import { StubPage } from '@/components/dashboard/StubPage';

export default function AccountsPage() {
  return (
    <StubPage
      phase="Phase 5"
      title="Accounts"
      description="Full table view of every connected account, with rename / hide / disconnect actions. Phase 5."
    />
  );
}
