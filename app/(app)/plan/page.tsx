import { StubPage } from '@/components/dashboard/StubPage';

export default function PlanPage() {
  return (
    <StubPage
      phase="Coming soon"
      title="Plan"
      description="The plan view is where Beacon will walk you through actions, track what you have done, and tee up the next move. For now, Beacon's brief on the home dashboard surfaces the most actionable things."
    />
  );
}
