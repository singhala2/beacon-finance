import { StubPage } from '@/components/dashboard/StubPage';

export default function PlanPage() {
  return (
    <StubPage
      phase="Phase 5"
      title="Plan"
      description="The plan view is where Beacon walks you through actions, tracks what you have done, and tees up the next move. Lands in Phase 5."
    />
  );
}
